version: 2

sources:
  - name: monzo_datawarehouse
    description: >
      Raw backend tables from Monzo’s append-only event logs.
      Fully refreshed nightly and may change over time.

    config:
      project: analytics-take-home-test
      dataset: monzo_datawarehouse

    tables:
      - name: account_created
        description: Account creation events (one row per account)

      - name: account_closed
        description: Account closure events (append-only)

      - name: account_reopened
        description: Account reopen events (append-only)

      - name: account_transactions
        description: Daily aggregated transaction counts per account
